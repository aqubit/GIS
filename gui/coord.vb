Imports ESRI.ArcGIS.Geodatabase

Public Class coord

    Private Sub btnOK_Click(ByVal sender As System.Object, ByVal e As System.EventArgs) Handles btnOK.Click
        'Dim pfcsal As IFeatureClass
        'Dim pfcent As IFeatureClass
        'Dim arEQRutas As New ArrayList
        'Dim arEQBuffer As New ArrayList
        'pfcsal = Herramientas.AbrirFCxWS(pWS, "colegios_temp")
        'pfcent = Herramientas.AbrirFCxWS(pWS, "Colegio")
        'Dim lisID As New ArrayList
        'lisID.Add(368)
        'lisID.Add(420)
        ''lisID.Add(363)
        'arEQRutas = Herramientas.encontrarFeatures(lisID, pfcent, pfcsal, 1)
        'arEQBuffer = Herramientas.encontrarFeatures(lisID, pfcent, pfcsal, 2)
        'Herramientas.BorrarFeatureClass("colegios_temp")
    End Sub
End Class